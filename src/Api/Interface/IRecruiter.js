/**
 * @typedef IJobDescription
 * @prop {string} description because someone won't understand if I use string array
 */

/**
 * @typedef IRecruiter
 * @prop {number} id
 * @prop {string} name
 * @prop {number} rating
 * @prop {string} description
 * @prop {string} avatar
 * @prop {IJobDescription[]} jobs
 */
